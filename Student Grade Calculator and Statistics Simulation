package midtermanswer;

public class MidtermAnswer {

    public static void main(String[] args) {
        // TODO code application logic here
        int countA = 0 , countB = 0 , countC = 0 , countD = 0 , countF = 0;
        int countFF = 0 , countAA = 0;
        int absentee = 0;
        int sum = 0;
        double average = 0;
        
        for(int i = 0; i<80; i++){
            int randomStatus = (int)(Math.random()*2);
            if(randomStatus == 1){
                int randomNotes = (int)(Math.random()*101);
                if(randomNotes > 89 && randomNotes <= 100){
                    System.out.printf("%d. students score : %d , letter grade is A\n" , i+1 ,randomNotes);
                    countA++;
                    if(randomNotes == 100){
                        countAA++; 
                    }
                }
                else if(randomNotes < 90 && randomNotes > 79){
                    System.out.printf("%d. students score : %d , letter grade is B\n" , i+1 ,randomNotes);
                    countB++;
                }
                else if(randomNotes < 80 && randomNotes > 69){
                    System.out.printf("%d. students score : %d , letter grade is C\n" , i+1 , randomNotes);
                    countC++;
                }
                else if(randomNotes < 70 && randomNotes > 59){
                    System.out.printf("%d. students score : %d ,  letter grade is D\n" , i+1 ,randomNotes);
                    countD++;
                }
                else{
                    System.out.printf("%d. students score : %d ,  letter grade is F\n" , i+1 , randomNotes);
                    countF++;
                    if(randomNotes == 0){
                        countFF++;
                    }
                    if(randomNotes == 0 || randomNotes == 100){
                        continue;
                        
                    }
                   
                }
              sum+=randomNotes;
            }
            else{
                System.out.printf("%d. students condition : absentee \n", i+1);
                absentee++;
            }
        }
        average = (double)sum/(countA + countB + countC + countD + countF - countAA - countFF);
        System.out.println();
        System.out.println("The number of absent students : " + absentee);
        System.out.println("The number of grade A : " + countA);
        System.out.println("The number of grade B : " + countB);
        System.out.println("The number of grade C : " + countC);
        System.out.println("The number of grade D : " + countD);
        System.out.println("The number of grade F : " + countF);
        System.out.printf("The average of the exam is : %.2f\n", average);
    }
    
}
